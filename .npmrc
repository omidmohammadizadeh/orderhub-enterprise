# Build tooling (turbo, tsc, @nestjs/cli) lives in devDependencies.
# Render sets NODE_ENV=production, which makes pnpm skip devDependencies and
# breaks the build ("turbo: not found"). Force them to install.
prod=false
