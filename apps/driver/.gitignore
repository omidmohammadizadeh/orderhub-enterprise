node_modules/
.expo/
dist/
web-build/
ios/
android/
*.log
.env*
# google-services.json is committed (client config, not a secret) so EAS Build
# bundles it — EAS applies .gitignore patterns even to tracked files.
GoogleService-Info.plist
